# 草稿
## 初始化
目的：
项目初始化，需要建立文件结构
这个项目是为了制作一个prompt标准化工具，帮助人快速准确的生成prompt，并记录这些项目
他应该是基于flask和静态网页完成的

步骤：
1.建立文件夹src，data，etc，pyproject.toml
2.项目通过uv建立虚拟环境并执行代码
3.data应该被。gitignore掉。

将来在APP设计上，你可以参考../HappyToGo 
文件夹结构../Zhixiang

不要猜测我的意图。
有任何不懂向我确认。

我觉得static templates app.py都应该进入src中，这样比较清爽

我现在默认的port是2333，为什么没有.venv文件夹呢

## 界面设计
目标：
设计用户界面

步骤：
这个界面分成左右两部分。左边是是prompt的表单
包括五部分
目标：
输入：
输出：
步骤：
备注：
其中，目标和备注可以一次填写。
输入：
输出：
步骤：
要分条填写。
右侧暂时空白，我会逐渐增加。不过对应 备注： 这个部分，有一个备注常驻内容栏。它填写后，每次生成prompt都应该包括这个内容。

这个附加要求属于是备注的一部分。如果填写了就在备注下方显示。

## 备注
不要推测我的意图。
有任何不懂都要问我。

## 目标
设计prompt持久化方案。
每条prompt都应该结构化存在一个yaml中。
不过在那之前应该为该app增加一个功能。在最上面加一个栏，这个栏最左边加一个功能，叫做创建项目。
这个创建项目包括项目名称，他会在data中创建一个文件夹，名字为项目名称。
这个文件夹会包括一个yaml，负责记录需要被存档的prompt。这里的逻辑之后我们再说。
另外，这个文件夹，还有有另一个yaml，记录着一些关于这个项目的一些配置。备注常驻内容就是其中之一。

## 备注
不要推测我的意图。
有任何不懂都要问我。

## 目标
现在开始设计生产Prompt的逻辑。
在备注下，还应该有一个对话框，点击生成Prompt之后，
那里应显示完整的prompt。
然后生成Prompt会变成复制Prompt
这个复制Prompt点击表示认可了这个Prompt。他应该被存入yaml中去
如果没有被点击，而表单内容被修改则复制Prompt重新变成生成Prompt
先更改界面，然后我们讨论yaml的结构

## 备注
不要推测我的意图。
有任何不懂都要问我。

## 目标
yaml可以按照条目顺序保存每条prompt
每个item有五个条目
然后
输入
输出
步骤
有 下一集内容

## 备注
不要推测我的意图。
有任何不懂都要问我。